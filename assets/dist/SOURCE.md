# Sentient Forms Admin App Source

The JavaScript and CSS files in this directory are generated from the SvelteKit admin app source for Sentient Forms 0.2.0.

Source included in the WordPress.org package: admin-app

Build commands:

```sh
cd admin-app
bun install --frozen-lockfile
bun run restore:source
bun run build:wp
```
